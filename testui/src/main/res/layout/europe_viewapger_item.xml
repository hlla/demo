<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/scroll_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:scrollbarAlwaysDrawVerticalTrack="false"
            android:scrollbarThumbVertical="@drawable/scroll_bar_white_indicator"
            android:scrollbars="vertical">

    <LinearLayout
        android:id="@+id/ll_europe_viewpager_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/splash_content_margin_horizontal"
        android:layout_marginRight="@dimen/splash_content_margin_horizontal"
        android:orientation="vertical"/>
</ScrollView>
